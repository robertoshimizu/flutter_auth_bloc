import 'package:flutter/material.dart';

class RankingNamePage extends StatelessWidget {
  const RankingNamePage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Nome"),
    );
  }
}
