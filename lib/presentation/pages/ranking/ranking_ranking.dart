import 'package:flutter/material.dart';

class RankingRankingPage extends StatelessWidget {
  const RankingRankingPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Ranking"),
    );
  }
}
