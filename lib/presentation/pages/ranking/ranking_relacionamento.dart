import 'package:flutter/material.dart';

class RankingRelacionamentoPage extends StatelessWidget {
  const RankingRelacionamentoPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Relacionamento"),
    );
  }
}
